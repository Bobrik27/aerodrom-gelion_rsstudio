# Журнал Разработки

*Для общего обзора проекта, см. файл [PROJECT_BRIEF.md](PROJECT_BRIEF.md).*

---

### **Дата: 2025-11-03**

**Цель сессии:** Инициализация проекта, настройка окружения и создание базовой структуры сайта.

**✅ Выполнено:**
1.  Создана основа проекта с помощью `create-next-app` (TypeScript, Tailwind, App Router).
2.  Создана кастомная структура папок: `docs`, `analysis`, `src/components`, `src/lib`.
3.  Инициализирован Git-репозиторий и проект успешно отправлен на GitHub.
4.  Созданы базовые компоненты `Header.tsx` и `Footer.tsx`.
5.  Главный шаблон `layout.tsx` очищен и настроен для использования `Header` и `Footer` на всех страницах.
6.  Главная страница `page.tsx` очищена от стандартного контента.
7.  Создана система документации проекта (`PROJECT_BRIEF.md` и `DEV_LOG.md`).
8. Проведен полный аудит сайта-предшественника и задокументирован в analysis/LEGACY_SITE_AUDIT.md.
9. Принято архитектурное решение о добавлении раздела "Блог" для повышения "живости" сайта и SEO.

**🤔 Принятые решения:**
- Решено разделить рабочие материалы (`docs`, `analysis`) и исходный код сайта (`src`, `public`). Это позволит держать проект в одном месте, но на хостинг отправлять только чистый код после сборки (`npm run build`).

**❗️ Проблемы и их решение:**
- `create-next-app` не смог запуститься в папке с заглавными буквами. **Решение:** папка проекта была переименована в `airport-proj`.
- `mkdir` в PowerShell требует другого синтаксиса для создания нескольких папок. **Решение:** использовали `mkdir dir1, dir2`.
- `git add .` и `git commit` изначально не видели файлы. **Решение:** полностью удалили папку `.git` и инициализировали репозиторий заново. Это решило проблему.
- Возникла сетевая ошибка `Connection timed out` при `git push`. **Решение:** проблема решилась сама собой после повторной попытки, вероятно, была связана с временными неполадками сети или брандмауэром.

**▶️ Следующие шаги:**
- Создание навигационного меню в `Header`.
- Проектирование основной секции на главной странице.

---